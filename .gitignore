__pycache__/
*.pyc
checkpoints/
logs/
results.csv
# ICLR paper draft (local only, not pushed)
paper/
imagenet100
pilot_in100/
# datasets/: keep the folder skeleton + README, ignore the actual (huge) data
datasets/*
!datasets/README.md
# relctl runtime state (jobs registry, generated overlays). Saved profiles under
# relctl/profiles/ ARE tracked so experiments are reproducible/shareable.
.relctl/

# Virtual environments
.venv/
venv/
env/

# IDE / OS
.idea/
.vscode/
.DS_Store
Thumbs.db
